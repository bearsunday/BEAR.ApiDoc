name: Test API Documentation Workflow

on:
  push:
    branches: [deploy]

jobs:
  test-html:
    uses: ./.github/workflows/apidoc.yml
    with:
      format: 'html'
      docs-path: 'docs/api'
      publish-to: 'artifact-only'
