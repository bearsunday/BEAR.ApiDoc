{
  "$id": "ticket.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ticket",
  "description": "Support ticket for tracking customer issues and requests",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the ticket",
      "example": "TKT-2024-001"
    },
    "title": {
      "type": "string",
      "description": "Brief summary of the issue or request",
      "example": "Cannot login to dashboard",
      "minLength": 3,
      "maxLength": 255
    },
    "description": {
      "type": "string",
      "description": "Detailed explanation of the issue including steps to reproduce",
      "example": "When I click the login button, the page shows a 500 error.",
      "maxLength": 2000
    },
    "assignee": {
      "type": "string",
      "description": "Username of the support agent handling this ticket",
      "example": "john.smith",
      "maxLength": 50
    },
    "status": {
      "description": "Current status of the ticket in the workflow",
      "type": "string",
      "enum": ["open", "in_progress", "waiting", "resolved", "closed"],
      "example": "in_progress"
    },
    "priority": {
      "description": "Urgency level of the ticket",
      "type": "string",
      "enum": ["low", "medium", "high", "critical"],
      "example": "high"
    },
    "created": {
      "description": "Timestamp when the ticket was created",
      "type": "string",
      "format": "date-time",
      "example": "2024-01-15T09:30:00Z"
    },
    "updated": {
      "description": "Timestamp when the ticket was last modified",
      "type": "string",
      "format": "date-time",
      "example": "2024-01-16T14:22:00Z"
    }
  },
  "required": ["title", "description", "status", "created", "updated"],
  "additionalProperties": false
}
